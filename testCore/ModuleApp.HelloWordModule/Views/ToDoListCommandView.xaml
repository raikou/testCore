<UserControl x:Class="testModuleAppPrism.Views.ToDoListCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:testModuleAppPrism.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewModels="clr-namespace:testModuleAppPrism.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
			 <UserControl.DataContext>
				<viewModels:ToDoListCommandViewModel/>
			 </UserControl.DataContext>
    <Grid>
	    <StackPanel>
			<DataGrid  ItemsSource="{Binding GridItem ,Mode=TwoWay}" Height="100px" IsReadOnly="True"/>
		    <Button Content="データ取得" Command="{Binding GetMainListCommand}" />
		    <Button Content="追加" Command="{Binding Add}" />
			<Button Content="更新" Command="{Binding Upd}" />
			<Button Content="削除" Command="{Binding Del}" />
	    </StackPanel>
    </Grid>
</UserControl>
